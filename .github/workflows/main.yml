name: Create envfile

on: [push]

jobs:

  create-envfile:
 
    runs-on: ubuntu-18.04
 
    steps:
    - name: Make envfile
      uses: SpicyPizza/create-envfile@v1
      with:
        envkey_DEBUG: false
        envkey_API_KEY: "46b4cd1c323b57f7a324a178d1423bd9d38c8df0"
        envkey_SECRET_KEY: ${{ x98Ds5%cRP5@fgzmP&4 }}
        some_other_variable: foobar
        directory: <directory_name>
        file_name: .env
